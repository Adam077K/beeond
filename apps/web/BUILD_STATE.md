# BUILD_STATE — checkpoint @ ~60% context (2026-07-04)

Branch `feat/landing-fable5` · worktree `.worktrees/landing-fable5` · app at `apps/web` · dev server on :3000 (background).

## Complete (committed)
1. `70de2a3` scaffold + Constitution (brand.lock.ts, motion.ts, theme lockdown, brand-lint zero, Rubik self-hosted latin+hebrew)
2. `df4c0c9` hero C8: SSR editorial split (3-line locked H1 + u-accent underline), founders ledger, header glass+deferred CTA, Chapter-1 canvas one-shot → rested SVG. Evidence r0/r1 + 5 motion frames in `build-evidence/`. Scores ≥3.5 logged.
3. `3b1989d` C7: sticky+scrub hive, 11 DoD-traceable channels, keystone (GEO locks last + pulse), ferry dots (shared glow identity), JS-off 11/11 proofs, mobile ledger, 60fps @4× CPU. Scores logged; round-2 pulse boost applied.

## In progress
- C7 round-2 keystone re-shot (pulse 0.85/1.55) → paste into BUILD_LOG.

## Next (order)
4. Task 8 — secondary sections, solid-but-simpler per rhythm map in BUILD_LOG: problem (zig-zag+panel quote) · how-it-works (uneven 3-node flight path, panel beat) · anti-generic (dense manifesto, 720) · C6 proof (editorial panels, panel beat) · founders deep (38/62 mirror + soft self-qualification) · C5 trust (real scores.json or DROP) · social proof ("we're selective" empty state) · FAQ (hex +/× morph, exits 0.8×) · final CTA (deep #0A0A0A, Chapter-3 rested hive, launch-hold footer Rubik 300 11px)
5. Task 9 — RTL: EN↔HE toggle (locked HE hero strings in brand.lock), logical-props already enforced; Playwright RTL shots (logo/numerals unmirrored)
6. Task 10 — craft: hex-hamburger mobile menu (exit 0.8×), OG card (satori via next/og, reversed Bee⬡nd), branded 404, cursor states, focus audit
7. Task 11 — gates: mobile Lighthouse ≥90/95 JSON, LCP/CLS/INP artifacts, JS-off crawl, e2e (@playwright/test, use data-p for scrub assert), brand-lint --dist, island kb sizes → motion ledger
8. Task 12 — fresh-eyes rhythm pass, BUILD_LOG finalize (flourish decision F1 flight-line vs F2 settle-dust — pick ONE), session file `docs/08-agents_work/sessions/2026-07-04-ceo-landing-fable5.md`, DECISIONS.md breadcrumb. NO merge — QA-Lead + founder gate.

## Open issues (one line each)
- Hero founder credential microlines = role-truth placeholders (launch-gate: founder supplies credential-dense lines)
- HE strings beyond locked hero flagged CMO-review (D4)
- Logo = interim v0.9 (founder vector pass pending)
- Header mobile: no menu until task 10 (nav anchors desktop-only meanwhile)
- Playwright MCP saves to ceo-1 worktree cwd — move artifacts to `apps/web/build-evidence/` after each capture

## Key facts
- Palette 7 tokens (muted in, pure white OUT — D2). Yellow fill-only; `bg-yellow` needs brand-lint-allow.
- Timeline duration pinned to 1 (`tl.set({},{},1)`) — positions are progress fractions; `data-p` instrumented.
- All copy from POSITIONING/OFFER_SPEC; H1/subhead/CTA verbatim from brand.lock COPY.
- Deviations D1–D8 logged in BUILD_LOG with rationale.
